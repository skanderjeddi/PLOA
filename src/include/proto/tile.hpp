#pragma once

/**
 * @brief Properties of a tile.
 */
enum class TileEdge;
enum class TileRotation;

/**
 * @brief A generic tile class.
 * 
 * @tparam V the type of the value of the tile
 */
template <typename V> class Tile;
